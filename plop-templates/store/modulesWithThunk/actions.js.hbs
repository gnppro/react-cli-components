import { standardAction, errorAction } from '../common/actions';
import types from './types';

const {
  {{constantCase name}}_REQUEST,
  {{constantCase name}}_SUCCESS,
  {{constantCase name}}_FAILURE,
  {{constantCase name}}_RESET,
} = types;

const do{{pascalCase name}}Request = (payload) => standardAction({{constantCase name}}_REQUEST, payload);
const do{{pascalCase name}}Success = (payload) => standardAction({{constantCase name}}_SUCCESS, payload);
const do{{pascalCase name}}Failure = (payload) => errorAction({{constantCase name}}_FAILURE, payload);
const do{{pascalCase name}}Reset = (payload) => standardAction({{constantCase name}}_RESET, payload);

export default {
  do{{pascalCase name}}Request,
  do{{pascalCase name}}Success,
  do{{pascalCase name}}Failure,
  do{{pascalCase name}}Reset,
};
